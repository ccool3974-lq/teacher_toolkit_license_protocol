name: teacher_toolkit_license_protocol
description: Shared license protocol package for Teacher Toolkit projects.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.11.4

dependencies:
  meta: ^1.17.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
